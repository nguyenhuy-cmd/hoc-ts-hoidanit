/*Named export(Xuất nhiều phần tử có tên riêng biệt) vs Default export(Chỉ định 1 phần tử làm
giá trị mặc định: nghĩa là chỉ xuất ra 1 biến thôi) của module*/
import { tru, sum } from "./math";
console.log(tru(10,3));
 