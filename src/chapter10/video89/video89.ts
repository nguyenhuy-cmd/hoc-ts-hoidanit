// Kiểu dữ liệu: Array

// Cách code 1:
const names: string[] = ["Huy", "Hà", "Lan"];
const score: number[] = [8,5,4];

// Cách code 2: generic
const names1: Array<string> = ["Huy", "Hà", "Lan"];
const score1: Array<number> = [8,5,4];
console.log(names);
console.log(names1);
console.log(score);
console.log(score1);
